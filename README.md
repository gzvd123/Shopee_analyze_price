# Shopee Analyze Price UI Refresh

Giao diện được làm mới với phong cách glassmorphism, hỗ trợ Light/Dark mode và tách riêng các tài nguyên để dễ bảo trì.

## Cách chạy
1. Mở trực tiếp file `index.html` bằng trình duyệt hiện đại (Chrome/Edge/Firefox).
2. Công cụ tự lưu cấu hình, sản phẩm và chế độ hiển thị vào LocalStorage để lần sau mở lại không mất dữ liệu.

## Điểm nổi bật
- **Theme linh hoạt:** Nút chuyển `Light/Dark` ở thanh công cụ, lưu trạng thái vào LocalStorage.
- **Bố cục trong suốt hiện đại:** Nhiều lớp gradient, blur và thẻ phân vùng rõ ràng.
- **Tách riêng tài nguyên:** CSS đặt tại `style.css`, logic JavaScript tại `app.js` giúp tổ chức mã chuyên nghiệp.
- **Nhấn mạnh trải nghiệm:** Header và thanh tiện ích mới, nút reset dữ liệu, hiệu ứng hover/animation tinh tế.

## Phím tắt/tiện ích
- `🌞/🌙` đổi theme.
- `🧹` làm mới dữ liệu và cấu hình lưu trữ (tải lại trang).

